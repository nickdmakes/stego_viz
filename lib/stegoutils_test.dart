import 'package:stego_viz/stegoutils.dart';
import 'package:image/image.dart' as img;

void main() async {
  // Uint8List imageBytes = Uint8List.fromList([0, 0, 0, 255, 255, 255]);
  // List<int> allBits = stringToBits('aabbc', singleBit: true, bit: 7);
  // print(allBits);
  // print(allBits.length);
  //
  // // make image from same directory
  // final bytes = await rootBundle.load('assets/images/lena.png');
  // img.Image image = img.decodeImage(Uint8List.view(bytes.buffer))!;
  //
  // print(image.width);
  // print(image.height);
  // print(stringToBits('aabbc'));
  //
  // List<img.Pixel> pixels = getPixelsLexicographically(image);
  //
  // for(int i = 0; i < 10; i++) {
  //   pixels[i].palette!.setRed(0, 255);
  // }

}