import 'package:flutter/material.dart';

class ColorEditor extends StatelessWidget {
  const ColorEditor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Color Editor'),
    );
  }
}
