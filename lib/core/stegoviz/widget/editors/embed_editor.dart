import 'package:flutter/material.dart';

class EmbedEditor extends StatelessWidget {
  const EmbedEditor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Embed Editor'),
    );
  }
}
