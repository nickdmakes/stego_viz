import 'package:flutter/material.dart';

class MessageEditor extends StatelessWidget {
  const MessageEditor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Message Editor'),
    );
  }
}
